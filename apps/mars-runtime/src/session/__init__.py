"""Session-layer modules for the Mars runtime supervisor.

* :mod:`session.claude_code_stream` — the JSONL parser that translates
  Claude Code ``stream-json`` output into typed Mars events (Story 1.2 +
  contract test in Story 1.3).
* Future: ``session.manager``, ``session.claude_code``,
  ``session.permissions``.
"""
